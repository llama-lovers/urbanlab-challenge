[project]
name = "hackathon-pipeline"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "polars>=1.14",
    "pyarrow>=18.0",
    "geopandas>=1.0",
    "click>=8.1",
    "psycopg2-binary>=2.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
