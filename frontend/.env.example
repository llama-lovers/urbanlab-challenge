# UrbanLab backend (auth + chat sessions + SSE streaming). See backend/openapi.json.
NEXT_PUBLIC_API_URL=http://10.8.47.10:8000
# Whisper speech-to-text service (POST /asr). Used for voice input in the composer.
NEXT_PUBLIC_WHISPER_URL=http://10.8.47.10:9000
